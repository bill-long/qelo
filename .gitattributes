# Normalize line endings to LF in the repo and working tree.
# Keeps Windows checkouts from fighting Biome (which formats with LF).
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.ico binary
*.icns binary
*.svg text eol=lf
